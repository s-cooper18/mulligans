[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mulligans"
version = "0.1.0"
description = "A Python project"
dependencies = ["attrs", "mtg-parser==0.0.1a55"]

[project.optional-dependencies]
dev = [
    "mypy",
    "ruff",
]
